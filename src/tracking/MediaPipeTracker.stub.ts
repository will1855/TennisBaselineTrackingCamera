/**
 * MediaPipeTracker — STUB (not used in v1)
 *
 * This module is a placeholder for a future MediaPipe-based pose tracker.
 *
 * When ready to implement:
 *  1. Install: npm install @mediapipe/pose @mediapipe/camera_utils
 *  2. Initialise a MediaPipe Pose solution with a <video> element.
 *  3. On each pose result, read landmarks[23] (left hip) and landmarks[24] (right hip).
 *  4. Compute the midpoint as the player's horizontal position.
 *  5. Map to TargetState.x (0–1) using the frame width.
 *  6. Use pose.score as confidence.
 *
 * References:
 *  - https://google.github.io/mediapipe/solutions/pose.html
 *  - https://developers.google.com/mediapipe/solutions/vision/pose_landmarker
 *
 * This class deliberately exports nothing functional in v1.
 */

export {};
